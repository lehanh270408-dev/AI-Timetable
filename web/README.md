SPCN Web (Flask)

Chạy giao diện web cho dự án sắp xếp TKB và gợi ý lớp.

Hướng dẫn nhanh (PowerShell):

```bash
cd web
python -m venv .venv
. .venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```

Mặc định chạy ở `http://localhost:5000`.


